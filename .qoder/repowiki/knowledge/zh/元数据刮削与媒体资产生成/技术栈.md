- **HTTP 客户端**：`curl_cffi` 用于带 TLS 指纹模拟的同步请求（绕过反爬），`aiohttp` 用于异步图片下载。
- **HTML 解析**：`beautifulsoup4` (lxml parser) 用于结构化数据提取。
- **图像处理**：`Pillow` 用于封面图的裁剪与格式转换。
- **LLM 集成**：支持通过环境变量接入 OpenAI 兼容 API 进行元数据翻译和复杂字段抽取。